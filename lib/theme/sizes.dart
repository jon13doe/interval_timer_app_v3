class CustomSizes {
  final double iconSize;
  final double padding;

  CustomSizes({
    required this.iconSize,
    required this.padding,
  });
}

// small
// medium
CustomSizes medium = CustomSizes(
  iconSize: 24,
  padding: 5,
);
// big